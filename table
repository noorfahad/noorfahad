#include <stdio.h>
int main()
{
    
    int num;
    int i=1;
    
    
    printf("Enter the number to display table: ");
    scanf("%d", &num);
    
    for (i=1; i<=10; i++)
    
    {
        printf("%dx%d=%d\n", num, i, num*i);
        
        
    }
    
    
    return 0;
    
}
